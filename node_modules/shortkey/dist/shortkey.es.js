var shortkey = function shortkey(handlers) {
  return function (event) {
    var name = ['on', event.ctrlKey ? 'Ctrl' : null, event.altKey ? 'Alt' : null, event.shiftKey ? 'Shift' : null, event.metaKey ? 'Meta' : null, event.key].filter(Boolean).join('');
    var handler = handlers[name];

    if (handler) {
      handler(event);
    }
  };
};

export default shortkey;
